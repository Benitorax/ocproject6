<h1>Welcome {{ email.toName }}!</h1>

<p>
    You signed up as {{ email.toName }} the following email:
</p>
<p><code>{{ email.to[0].address }}</code></p>

<p>
    <a href="{{ url('user_activate', {'token': token.token}) }}">Click here to activate your account</a>
    (this link is valid until {{ token.expiredAt|date('F jS') }})
</p>